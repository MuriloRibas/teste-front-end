import React from 'react';

const styles = {
    position: 'fixed',
    bottom: '5%',
    left: '45%',
};

const Loading = () => (
    <strong style={styles}>
        Carregando...
    </strong>
);

export default Loading;
